# Используем официальный образ Golang в качестве базового образа
FROM golang:latest

# Установка рабочей директории внутри контейнера
WORKDIR /app

# Копирование файлов go.mod и go.sum в рабочую директорию
COPY go.mod .
COPY go.sum .

# Загрузка зависимостей модуля Go
RUN go mod download

# Копирование всех файлов проекта в рабочую директорию
COPY . .

# Сборка приложения
RUN make

# Указываем порт, который будет прослушиваться внутри контейнера
EXPOSE 8080

# Запуск приложения при старте контейнера
CMD ["./main"]
